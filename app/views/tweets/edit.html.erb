<h1>Editing tweet</h1>
 
<%= form_for :tweet, url: tweet_path(@tweet), method: :patch do |f| %>
  <!-- error message -->
  <% if @tweet.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited
      this tweet from being saved:</h2>
    <ul>
    <% @tweet.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  
  <!-- form -->
  <p>
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
 
<%= link_to 'Back', tweets_path %>